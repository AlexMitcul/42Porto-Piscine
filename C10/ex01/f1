
i[200~o_cat(char *executable, char *path, int file_descriptor)
{
	unsigned int	byte_read;
	char			buffer[BUFFER_SIZE];

	while ((byte_read = read(file_descriptor, buffer, BUFFER_SIZE)) > 0)
	{
		if (errno != 0)
		{
			show_error(executable, path);
			break ;
