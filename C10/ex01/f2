
bool	cat(char **paths, int count, int offset)
{
	bool	result;
	int		file_descriptor;
	int		index;
	char	*path;

	result = true;
	index = offset;
	while (index < count + offset)
	{
		path = paths[index];
		file_descriptor = open(path,
